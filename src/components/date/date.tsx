import { Component } from '@stencil/core';

@Component({
  tag: 'adc-date',
  // styleUrl: 'my-component.css',
  shadow: true,
})
export class Date {
  // @Prop() first: string;
  // @Prop() middle: string;
  // @Prop() last: string;

  render() {
    return <div>Oh hello there</div>;
  }
}
